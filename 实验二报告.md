# 实验二报告：函数返回类型推断

## 1. 实验目标

本次实验在 teac 的 IR 生成前加入一个实验性返回类型推断 Pass，使省略返回类型标注的函数可以根据函数体中的 `return` 语句自动推断为 `i32` 或 `void`。该功能由 Cargo feature `return-type-inference` 控制，默认关闭，避免影响主线语义。

## 2. 实现位置

本次实现集中在 `src/experimental/return_infer.rs`。该模块通过 `ReturnInferPass` 实现 `ModulePass`，在 IR 生成的 Pass 2（注册函数签名）与 Pass 3（函数体生成）之间运行。

整体流程如下：

```text
Pass 1: use 语句处理
Pass 2: 注册全局变量、结构体、函数签名
Pass 2.5: return-type-inference 推断省略返回类型
Pass 3: 局部类型推断与 IR 生成
```

## 3. 核心设计

### 3.1 类型变量

对每个省略返回类型的函数分配一个类型变量 `TypeId`，并用 `Ty` 表示推断过程中的类型：

```rust
enum Ty {
    Concrete(Dtype),
    Var(TypeId),
}
```

这样函数调用可以在返回类型尚未确定时继续参与约束收集，支持跨函数调用链和自递归。

### 3.2 并查集约束求解

实现了 `UnionFind` 的 `bind`、`union` 与 `resolve`：

- `bind(α, T)`：将类型变量所在等价类绑定到具体类型。
- `union(α, β)`：合并两个类型变量的等价类。
- `resolve(α)`：查询最终具体类型。

每个等价类最多允许一个具体类型。如果同一等价类先后被绑定到不同类型，例如 `void` 与 `i32`，则返回 `Error::TypeMismatch`。

### 3.3 合一操作

所有类型相等关系都通过 `unify` 处理：

- `Concrete(T1)` 与 `Concrete(T2)` 必须相同；
- `Var(α)` 与 `Concrete(T)` 会调用 `bind`；
- `Var(α)` 与 `Var(β)` 会调用 `union`。

这使变量定义、赋值、分支合并、循环合并和返回语句都能统一使用同一套约束逻辑。

## 4. 约束收集

`Collector` 遍历所有函数体，而不仅是省略返回类型的函数体，因为显式返回类型函数也可能调用待推断函数。

主要规则：

- `return e;`：对当前函数的返回类型变量加入 `α_fn = typeOf(e)`。
- `return;`：加入 `α_fn = void`。
- `let x: T = e;`：加入 `T = typeOf(e)`。
- `let x = e;`：将 `x` 绑定为 `typeOf(e)`，可暂时是类型变量。
- `x = e;`：将左值已有类型与右值类型合一。
- `if/else` 与 `while`：将分支或循环体环境与原环境合一。
- 函数调用：若 callee 待推断，则返回 `Ty::Var(α_callee)`；否则返回 Registry 中的具体返回类型。

收集完成后，对所有 pending 函数调用 `resolve`。未被任何 return 约束确定的函数回退为 `void`，保持旧语义兼容；非 `void`/`i32` 结果会报 `UnsupportedReturnType`。

## 5. 测试结果

测试环境为 x86_64 Linux，端到端测试使用 `aarch64-linux-gnu-gcc` 交叉编译并通过 `qemu-aarch64` 运行。

已执行命令：

```bash
cargo check --features return-type-inference
cargo test -- --test-threads=1
cargo test --features return-type-inference -- --test-threads=1
```

结果：

- `cargo check --features return-type-inference`：通过。
- 默认测试：30 个用例全部通过。
- 开启返回类型推断：35 个用例全部通过，其中 `type_infer_1` 至 `type_infer_4` 为正向推断测试，`type_infer_5` 为冲突返回类型负测试。

## 6. 总结

本次实验通过类型变量和并查集实现了全局返回类型约束求解。实现保持在独立 feature-gated 模块内，不改变主线默认行为；开启 feature 后可以正确处理线性跨函数依赖、自递归、引用参数组合以及 `void`/`i32` 返回冲突诊断。
