# 实验四报告：AArch64 后端浮点支持

## 1. 实验目标

本次实验在实验三已经生成 `f32` 相关 IR 的基础上，补齐 teac 的 AArch64 汇编后端，使浮点程序能够完成“源码解析、IR 生成、汇编生成、链接运行、输出比对”的端到端流程。核心目标包括：支持 `f32` 寄存器分配、浮点算术与比较、`i32`/`f32` 类型转换、AAPCS64 浮点参数和返回值传递，以及跨函数调用时的浮点 caller-save 保护。Bonus 选择开放任务“端到端测评与合并回归稳定性改进”，不把实验三三选一特性的端到端通过情况重复计为 Bonus。

## 2. 后端整体流程

teac 的后端以函数为单位工作。`AArch64AsmGenerator` 先根据 AAPCS64 处理函数入口参数，再由 `FunctionGenerator` 将 IR 语句下降为带虚拟寄存器的 AArch64 指令，之后 `RegisterAllocator` 做活跃性分析、干扰图染色和 spill/reload，最后 `AsmPrinter` 输出汇编文本。

实验四的改动贯穿这条流水线。IR 中的 `FBiOp`、`FCmp`、`SIToFP`、`FPToSI` 和 `FloatConst` 会分别下降为 `FBinOp`、`FCmp`、`Scvtf`、`Fcvtzs` 和 `Fmov`。这些指令在寄存器分配后被重写为物理寄存器形式，再打印为合法的 AArch64 汇编。

## 3. 核心实现

### 3.1 浮点类型与寄存器类别

在 `src/asm/aarch64/types.rs` 中，`Dtype::F32` 映射到 `RegisterSize::S32`，表示单精度浮点寄存器 `s0` 至 `s31`。`RegisterSize::class()` 将 `S32` 归入 `RegisterClass::Fpr`，而 `W32`、`X64` 仍归入 `Gpr`。这样寄存器分配器可以根据虚拟寄存器宽度判断它属于整数寄存器组还是浮点寄存器组。

### 3.2 AAPCS64 浮点调用约定

在 `src/asm/aarch64/aapcs.rs` 中，`Dtype::F32` 被归类为 `ArgumentClass::Float`。参数分类时，整数参数使用 NGRN 计数器分配 `x0` 至 `x7`，浮点参数使用 NSRN 计数器分配 `s0` 至 `s7`，两类寄存器互不影响。

入口处，`src/asm/aarch64.rs` 的 `handle_arguments` 对 `ArgumentLocation::Fpr(n)` 发出 `Fmov`，把 `sN` 中的参数搬到虚拟寄存器。调用点中，`emit_fpr_arg` 把实参搬到 `sN`。返回值处理中，`return_inst` 和 `return_value_load` 对 `S32` 使用 `s0`，保证浮点返回值符合 AAPCS64。

### 3.3 浮点指令选择与汇编打印

`src/asm/aarch64/function_generator.rs` 负责把浮点 IR 语句下降为后端指令：

- `FBiOp` 下降为 `fadd`、`fsub`、`fmul`、`fdiv`；
- `FCmp` 下降为 `fcmp`，比较结果通过 NZCV 标志位供条件跳转使用；
- `SIToFP` 下降为 `scvtf sD, wN`；
- `FPToSI` 下降为 `fcvtzs wD, sN`；
- 浮点常量先以 IEEE-754 位模式物化，再通过 `fmov` 放入 `s` 寄存器。

`src/asm/aarch64/printer.rs` 补齐了这些指令的打印逻辑。浮点寄存器统一使用 `RegisterSize::S32` 打印为 `sN`，整数/浮点转换指令则分别使用 `wN` 和 `sN`，匹配 AArch64 指令格式。

### 3.4 寄存器分配与 spill

`src/asm/aarch64/register_allocator.rs` 将虚拟寄存器按 `RegisterClass` 分成两类分别建图染色：整数虚拟寄存器染色到 `x8` 至 `x15`，浮点虚拟寄存器染色到 `s18` 至 `s25`。两类寄存器文件在硬件上相互独立，因此不能混用同一张干扰图。

当浮点虚拟寄存器需要溢出时，分配器根据 `RegisterSize::S32` 申请 4 字节 spill 槽，并在重写阶段使用 `ldr sN` / `str sN` 进行 reload 和 spill。`FBinOp`、`FCmp`、`Scvtf`、`Fcvtzs`、`Fmov` 都补齐了对应的重写逻辑，保证源操作数和目标操作数分别使用正确的整数或浮点 scratch 寄存器。

### 3.5 caller-save 保护

浮点可分配池选择 caller-saved 的 `s18` 至 `s25`。如果一个函数使用浮点寄存器，`SaveCallerRegs` 和 `RestoreCallerRegs` 会在函数调用前后额外保存、恢复 `d18` 至 `d25`。使用 `d` 寄存器成对压栈和弹栈可以保持栈 16 字节对齐，同时覆盖 `s` 寄存器的低 32 位内容。

## 4. 实验三回归修复

合并实验四框架后，回归测试发现 `struct-method` 的 IR 测试失败，原因是 `impl` 方法没有在模块级注册为 `TypeName::method`，方法调用也没有把接收者作为第一个参数传入。

为保证实验三在合并后仍可通过，补充了以下修复：

- `src/ir/gen/module_gen.rs` 注册 `ImplDef` 中的方法，并为方法体生成 IR；
- `src/ir/gen/type_infer.rs` 在类型推断阶段解析 `receiver.method()` 和 `Self::method(...)`；
- `src/ir/gen/function_gen.rs` 在 IR 生成阶段把点语法接收者作为第一个实参传入；
- `src/ir/function.rs` 保存当前 impl 类型，供 `Self::` 调用解析使用。

其中 `self` 在 IR 函数签名中表示为 `ptr %Struct`。函数生成时将 `self` 直接绑定到这个结构体指针，而不是像普通参数一样再分配一层栈槽，避免 `self.field` 的 GEP 基址变成指针的指针。

## 5. Bonus 开放任务：端到端测评与合并稳定性

实验四 Bonus 选择的是对 teac 测评体系和合并后稳定性的开放改进，主要落在 `tests/tests.rs`：

- `ensure_cross_tools` 在测试开始前检查 `aarch64-linux-gnu-gcc`、`qemu-aarch64`、`docker` 或 `cc`，缺失时给出明确安装提示，避免测试失败表现为难定位的链接/运行错误。
- `ensure_std` 按平台生成并缓存 `tests/std/std-*.o`，仅在 `std.c` 更新后重编译，减少端到端测试的重复构建成本。
- `asmt_tests!` 统一实验一 AST、实验三 IR、实验四汇编三条测评路径。每个新增特性测试都会先跑 AST，再根据 `asmt-tests-ast` / `asmt-tests-ir` / 默认汇编路径进入对应阶段。
- Docker macOS 路径拆分为“链接”和“运行”两个容器阶段，保证 Intel macOS 也能稳定执行 Linux AArch64 测试。

这项 Bonus 不新增语法，而是改进实验四最容易出问题的端到端验证链路，使后端测试在 x86 Linux、AArch64 Linux 和 macOS 上更容易复现与定位问题。

## 6. 测试结果

已执行并通过以下检查：

```bash
cargo fmt --check
git diff --check
cargo check --features float
cargo check --features struct-method,asmt-tests-ir
cargo test --features float -- --test-threads=1
cargo test --features float,asmt-tests-ir -- --test-threads=1
cargo test --features struct-method,asmt-tests-ir -- --test-threads=1
cargo test --features for-loop,struct-method,multi-dim-array,asmt-tests-ir -- --test-threads=1
cargo test --features for-loop,struct-method,multi-dim-array -- --test-threads=1
```

结果均通过。其中 `float` 默认测试走实验四端到端汇编路径，覆盖 `float_basic`、`float_arith`、`float_cmp`、`float_cast` 和 `float_func`；`asmt-tests-ir` 覆盖实验三 IR 路径；`for-loop`、`struct-method`、`multi-dim-array` 的组合测试用于确认实验三功能在合并实验四后没有回归。

## 7. 总结

实验四完成了 `f32` 在 AArch64 后端的完整支持。实现中保持了整数路径和浮点路径的职责分离：前端和 IR 继续描述类型与操作语义，后端根据寄存器类别分别完成指令选择、调用约定处理、寄存器分配和汇编打印。Bonus 部分选择端到端测评与合并稳定性改进，合并后同时修复了实验三方法调用相关回归，最终验证了实验三 IR 路径和实验四汇编路径的正确性。
