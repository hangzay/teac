# TeaLang PJ1 简洁版设计与系统流程讲解

## 1. 这份讲解的目标

这份文档主要用于课堂展示，重点回答三个问题：

1. 我实现的核心设计思路是什么
2. `teac` 整个系统从输入到输出是怎么流动的
3. 测试用例是怎么被读取、编译、执行和校验的

与详细报告不同，这里尽量少讲代码细节，多讲结构和流程。

---

## 2. 我实现的核心内容

我主要实现了三部分：

1. 多维数组
2. 浮点类型 `f32`
3. 类型转换 `as`

后续为了把测试完全跑通，我还补上了：

4. `impl` 块与方法调用

但如果课堂重点放在我原本负责的设计上，建议主要讲前 3 个。

---

## 3. 我的整体设计思路

我这次实现不是“看到一个测试挂了就补一个规则”，而是按编译器前端的三层结构来做：

1. 先确定 AST 应该怎么表示
2. 再修改 Grammar，让新语法能被正确识别
3. 最后在 Parser 中把 Pest 解析树转换成 AST

也就是说，我更关注的是：

**新特性能不能自然接入原有系统结构。**

这样做的好处是：

- 代码组织更清晰
- 不容易写成很多特判
- 后续继续扩展语义分析和 IR 时更方便

---

## 4. 多维数组的简洁设计思路

### 4.1 设计目标

多维数组的目标不是只支持二维数组，而是支持“数组的元素仍然可以是数组”。

所以我采用的是递归设计，而不是给二维、三维分别造节点。

### 4.2 设计方法

核心思想是：

- 一层数组类型只记录两件事
  1. 元素类型
  2. 当前长度
- 元素类型本身仍然可以是数组类型

所以：

- `[i32; 4]` 是一维数组
- `[[i32; 4]; 3]` 是二维数组
- `[[[i32; 4]; 3]; 2]` 是三维数组

### 4.3 为什么这样设计

因为这样做：

- 结构统一
- 扩展自然
- Parser 不需要按维度写特判

我觉得这部分最值得讲的不是“我支持了二维数组”，而是：

**我用递归类型把多维数组变成了一种统一结构。**

### 4.4 对应代码文件

- Grammar：`src/tealang.pest`
- AST 类型定义：`src/ast/types.rs`
- AST 输出：`src/ast/display.rs`
- Parser 类型解析：`src/parser/decl.rs`

---

## 5. `f32` 的简洁设计思路

### 5.1 设计目标

`f32` 不是单纯增加一个浮点字面量，而是把浮点数作为语言中的一种正式类型接入类型系统。

也就是说，`f32` 应该能够出现在：

- 变量声明
- 函数参数
- 函数返回值
- 数组元素类型
- 引用参数内部类型

### 5.2 设计方法

我把 `f32` 和 `i32` 并列处理，都作为内建类型的一部分。

同时增加浮点字面量规则 `float_literal`，让它作为表达式里的基本原子单位。

### 5.3 这里最重要的考虑

这部分最容易出错的地方不是“有没有写 `f32`”，而是：

**PEG 语法里匹配顺序是否正确。**

比如 `3.14`：

- 如果先匹配整数 `num`
- 就会先吃掉 `3`
- 后面的 `.14` 就会出错

所以 `float_literal` 必须在 `num` 前面。

负浮点数也是同理，`-3.14` 必须优先匹配成负浮点，而不是先按负整数处理。

### 5.4 对应代码文件

- Grammar：`src/tealang.pest`
- AST 类型定义：`src/ast/types.rs`
- AST 表达式定义：`src/ast/expr.rs`
- AST 输出：`src/ast/display.rs`、`src/ast/tree.rs`
- Parser 表达式解析：`src/parser/expr.rs`
- Parser 类型解析：`src/parser/decl.rs`

---

## 6. `as` 类型转换的简洁设计思路

### 6.1 设计目标

`as` 的目标不是“多一个关键字”，而是让类型转换在表达式里有明确的结构和优先级。

### 6.2 设计方法

我没有把 `as` 直接混进普通算术表达式，而是单独加了一层：

- `expr_unit`
- `cast_expr`
- `arith_term`
- `arith_expr`

`as` 放在 `expr_unit` 之上、`arith_term` 之内。

### 6.3 为什么这样设计

因为这能保证：

```tea
a + b as i32
```

被理解为：

```tea
a + (b as i32)
```

也就是 `as` 的优先级高于普通算术运算。

如果不单独分层，而是把 `as` 直接塞进算术表达式，就很容易让表达式结构混乱。

### 6.4 对应代码文件

- Grammar：`src/tealang.pest`
- AST 表达式定义：`src/ast/expr.rs`
- AST 输出：`src/ast/display.rs`、`src/ast/tree.rs`
- Parser 表达式解析：`src/parser/expr.rs`

---

## 7. 这三部分共同体现的设计思想

这次实现里，我一直在坚持一个共同原则：

**尽量做成结构扩展，而不是局部补丁。**

具体表现为：

- 多维数组用递归类型，不做二维三维特判
- `f32` 进入类型系统，不只是多一个字面量
- `as` 单独插层，不和算术表达式混写

这其实是编译器项目里比较重要的一点：

不是“功能先堆上去”，而是让新功能进入已有分层结构。

---

## 8. teac 整个系统的数据流程

下面这部分是课堂里很容易加分的内容，因为它体现你不只是盯着某几个规则，而是理解整个编译器怎么跑起来。

## 8.1 总体流程

`teac` 的主流程是：

```text
.tea 源文件
  -> 读取文件内容
  -> Parser 解析成 AST
  -> IR Generator 生成中间表示
  -> 优化 Pass
  -> AArch64 汇编
  -> 输出到 stdout 或文件
```

如果使用 `--emit ast`，就会在 AST 阶段提前停止，只输出 AST。

### 对应代码文件

- 编译器主入口：`src/main.rs`
- 通用生成器接口：`src/common.rs`
- Parser 总入口：`src/parser/mod.rs`
- IR 生成：`src/ir/`
- 优化：`src/opt/`
- 汇编生成：`src/asm/`

---

## 8.2 从输入文件到 AST

主入口在 `src/main.rs`。

系统先做两件事：

1. 解析命令行参数
2. 读取输入源文件内容

然后创建：

- `parser::Parser::new(&source)`

接着调用：

- `generate()`

生成 AST。

如果命令行是：

```bash
teac xxx.tea --emit ast
```

那么程序会在 AST 生成后直接输出，不再继续进入 IR 和汇编阶段。

### 对应代码文件

- 主流程：`src/main.rs`
- `Generator` trait：`src/common.rs`
- Parser 外层接口：`src/parser/mod.rs`

---

## 8.3 Parser 内部是怎么工作的

Parser 内部流程可以理解成两步：

1. Pest 先根据 `src/tealang.pest` 生成一棵解析树
2. `ParseContext` 再把这棵解析树转换成项目自己的 AST

也就是说，Pest 负责“识别语法”，项目自己的 Parser 负责“构造 AST”。

其中：

- 顶层入口在 `src/parser/mod.rs`
- 声明和类型相关解析在 `src/parser/decl.rs`
- 表达式解析在 `src/parser/expr.rs`
- 语句解析在 `src/parser/stmt.rs`
- 错误处理和辅助函数在 `src/parser/common.rs`

### 这部分课堂上可以怎么讲

你可以说：

> teac 不是直接从文本手写到 AST，而是先由 Pest 根据 `tealang.pest` 产生解析树，再由 `ParseContext` 分模块把不同语法节点转换成 AST。所以我做功能扩展时，实际上是在同步修改 Grammar、AST 和 Parser 三层。

---

## 8.4 AST 之后发生什么

如果不是 `--emit ast`，系统会继续：

1. 读取 Parser 生成的 AST
2. 交给 IR 生成器
3. 对 IR 跑默认优化流水线
4. 再生成 AArch64 汇编

这部分虽然不是本次 Parser 实验的重点，但理解这条链路很重要，因为它说明：

- AST 不是最终结果
- AST 是后续 IR 和代码生成的输入

### 对应代码文件

- AST 到 IR：`src/ir/`
- 优化 Pass：`src/opt/`
- 汇编生成：`src/asm/`
- 整体调度：`src/main.rs`

---

## 9. 测试系统是怎么工作的

## 9.1 测试入口在哪里

所有集成测试的主逻辑都在：

- `tests/tests.rs`

这个文件既负责：

- 检查外部工具是否存在
- 编译标准库 `tests/std/std.c`
- 调用 `teac`
- 运行生成的程序
- 比较输出结果

所以从系统角度看，`tests/tests.rs` 本身就是一个完整的测试驱动器。

---

## 9.2 测试用例文件怎么组织

每个测试目录通常长这样：

```text
tests/<test_name>/
  <test_name>.tea   // 源程序
  <test_name>.in    // 输入（可选）
  <test_name>.out   // 期望输出
```

比如：

- `tests/array_2d_basic/array_2d_basic.tea`
- `tests/float_cast/float_cast.tea`
- `tests/for_basic/for_basic.tea`

标准库支持文件位于：

- `tests/std/std.c`

标准库头文件在各测试目录中通过：

- `std.teah`

配合 `use std;` 使用。

### 对应代码文件

- 测试驱动：`tests/tests.rs`
- 测试样例目录：`tests/`
- C 标准库：`tests/std/std.c`

---

## 9.3 AST 测试是怎么跑的

AST 测试使用的是：

- `test_ast_parse(...)`

它的基本逻辑是：

1. 找到 `tests/<name>/<name>.tea`
2. 调用 `teac --emit ast`
3. 检查：
   - 进程退出码是否为 0
   - stderr 是否为空
   - stdout 是否非空
   - stdout 是否包含关键标识符

也就是说，这类测试并不要求 AST 输出格式一模一样，而是要求：

**能正确解析，并且输出中能体现关键结构。**

### 对应代码文件

- AST 测试入口：`tests/tests.rs`
- AST 输出实现：`src/ast/display.rs`、`src/ast/tree.rs`
- `--emit ast` 调度：`src/main.rs`

---

## 9.4 端到端测试是怎么跑的

完整测试不是只看能不能 parse，而是会真的把 TeaLang 程序编译并运行。

整体流程是：

1. 用 `teac --emit asm` 把 `.tea` 编译成 `.s`
2. 把 `tests/std/std.c` 编译成目标文件
3. 将 `.s` 和标准库目标文件链接成可执行文件
4. 如果测试目录里有 `.in`，就把它作为程序输入
5. 运行程序
6. 把实际输出与 `.out` 进行比较

所以 `.out` 不是“编译器输出”，而是：

**测试程序运行后的标准输出 + 退出结果的黄金答案。**

### 对应代码文件

- 端到端测试主逻辑：`tests/tests.rs`
- `teac --emit asm`：`src/main.rs`
- 汇编生成：`src/asm/`

---

## 9.5 为什么之前需要安装额外环境

测试文件里写得很清楚：

- 在 x86 / x86_64 Linux 上
- 需要 AArch64 交叉编译器
- 以及 QEMU 来运行 ARM64 程序

所以之前缺少：

- `aarch64-linux-gnu-gcc`
- `qemu-aarch64`

就会导致完整测试失败。

这不是 Parser 逻辑错误，而是运行测试的系统环境缺失。

### 对应代码文件

- 环境检测：`tests/tests.rs`

---

## 10. 每个关键部分在哪个文件

下面这张表适合课堂讲解时快速定位。

| 部分 | 代码文件 |
|------|----------|
| 编译器主入口 | `src/main.rs` |
| 通用 `Generator` 接口 | `src/common.rs` |
| Pest 语法定义 | `src/tealang.pest` |
| Parser 总入口 | `src/parser/mod.rs` |
| Parser 公共工具 | `src/parser/common.rs` |
| 声明/类型解析 | `src/parser/decl.rs` |
| 表达式解析 | `src/parser/expr.rs` |
| 语句解析 | `src/parser/stmt.rs` |
| AST 类型定义 | `src/ast/types.rs` |
| AST 表达式定义 | `src/ast/expr.rs` |
| AST 语句定义 | `src/ast/stmt.rs` |
| AST 声明定义 | `src/ast/decl.rs` |
| AST 顶层程序结构 | `src/ast/program.rs` |
| AST 文本输出 | `src/ast/display.rs` |
| AST 树形输出 | `src/ast/tree.rs` |
| IR 生成 | `src/ir/` |
| 优化 Pass | `src/opt/` |
| 汇编生成 | `src/asm/` |
| 集成测试主逻辑 | `tests/tests.rs` |
| 测试样例目录 | `tests/<case_name>/` |
| 测试标准库 | `tests/std/std.c` |

---

## 11. 课堂展示时建议怎么讲

如果你要更简洁地讲，建议按下面顺序：

### 第一部分：我做了什么

我主要做的是多维数组、`f32` 和 `as` 的 Parser 扩展，后面又补上了 `impl`，最后把测试全部跑通。

### 第二部分：我怎么设计

我的思路是先设计 AST，再改 Grammar，再写 Parser。
多维数组用递归类型表示；
`f32` 重点处理 PEG 的匹配顺序；
`as` 重点处理表达式优先级，所以单独加了一层 `cast_expr`。

### 第三部分：整个系统怎么跑

`teac` 从 `.tea` 文件读入源码，先经 Pest 和 Parser 变成 AST，如果是 `--emit ast` 就直接输出；否则继续生成 IR、优化，再输出汇编。
测试系统会读取 `tests/<name>/<name>.tea`，调用 `teac` 编译，必要时链接标准库并运行程序，再把输出和 `.out` 比较。

### 第四部分：可以体现理解深度的一句总结

我这次不是只改了几个规则，而是把新语法接进了 `Grammar -> AST -> Parser -> 测试` 这整条链路里。

---

## 12. 最后一句总结

如果只用一句话概括这次实现和系统理解，可以说：

**我既完成了多维数组、`f32`、`as` 的前端设计，也理解了 teac 从源码输入、Parser 构建 AST、到测试驱动编译运行的完整数据流程。**
